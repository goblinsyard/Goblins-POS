{
  "framework": "vite",
  "installCommand": "cd apps/pos && sed -i \"s/\"workspace:\\*\"/\"*\"/g\" package.json && npm install --legacy-peer-deps",
  "buildCommand": "cd apps/pos && ./node_modules/.bin/vite build",
  "outputDirectory": "apps/pos/dist"
}