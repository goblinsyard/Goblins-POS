{
  "framework": "vite",
  "installCommand": "npm install -g pnpm@8 && npm_config_registry=https://registry.npmjs.org/ pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @goblins/shared build && pnpm --filter @goblins/pos build",
  "outputDirectory": "apps/pos/dist"
}