{
  "name": "goblins-pos",
  "version": "0.1.0",
  "private": true,
  "description": "Goblins Yard POS monorepo",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.1",
    "typescript-eslint": "^8.61.0"
  }
}